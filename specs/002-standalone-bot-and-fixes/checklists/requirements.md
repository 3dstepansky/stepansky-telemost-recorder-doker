# Чек-лист проверки требований: Автономный Telegram-бот и фиксы (002-standalone-bot-and-fixes)

## Полнота и независимость сценариев
- [x] Присутствуют ли все приоритетные User Stories (P1/P2/P3)?
- [x] Можно ли независимо протестировать очистку зомби-процессов? (`docker exec ps aux | grep defunct`)
- [x] Описаны ли конкретные шаги для проверки диаризации?
- [x] Описаны ли сценарии автоматического выхода из встречи (организатор завершил, бот один 2 мин)?
- [x] Описаны ли требования к информационному стилю Ильяхова?

## Статус реализации
- [x] US-1 (Запись WebRTC аудио) — реализовано.
- [x] US-2 (Anti-Zombie) — реализовано.
- [x] US-3 (Telegram-бот) — реализовано.
- [x] US-4 (Автовыход) — реализовано.
- [x] US-5 (Инфостиль Ильяхова) — реализовано.
- [x] US-6 (Транскрибация с диаризацией) — реализовано (AssemblyAI с переходом на Groq Whisper). Проверено наличие `speaker_labels: true`.
- [x] US-7 (Выгрузка на Яндекс.Диск + S3) — реализовано.
- [x] US-8 (ИИ-саммари и рассылка в Telegram) — реализовано. Саммари рассылается пользователю после транскрибации.
- [x] US-16 (Нативная диаризация по трекам) — реализовано (Dual-Output).
- [ ] US-12 (Яндекс.Календарь) — не начато.

## Технические требования
- [x] Понятно ли, какая база данных используется? (SQLite — `telemost_bot.sqlite`)
- [x] Описан ли механизм предотвращения зомби-процессов? (`dumb-init` как PID 1)
- [x] Указан ли формат конвертации перед диаризацией? (`.webm` → `.mp3`)
- [x] Описан ли fallback при ошибке AssemblyAI? (Groq Whisper-large-v3, чанки по 10 мин)
- [x] Описан ли порог idle-выхода? (`MAX_IDLE_MINS=2`)

## Критерии успеха
- [x] Заданы ли измеримые метрики отсутствия зомби-процессов? (0 после 5 итераций)
- [x] Заданы ли метрики успешности диаризации? (>80% текста с метками)
- [x] Задан ли критерий для инфостиля? (Отсутствие `━` и субъективных суждений)
- [x] Задан ли критерий своевременности рассылки саммари? (SC-009: в течение 60 сек после транскрибации — реализовано)

## Неоднозначности и вопросы
- [x] **[RESOLVED]** Передаётся ли `speaker_labels: true` в API AssemblyAI? Да, передаётся по умолчанию.
- [x] **[RESOLVED]** Как передавать `chat_id` в `transcribe.js`, чтобы он мог отправить уведомление? Передаётся через аргумент командной строки (spawn) из `run.js`.
